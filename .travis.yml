language: cpp
sudo: required
dist: trusty
compiler:
- clang
os:
- linux
before_install:
- echo $LANG
- echo $LC_ALL
- llvm-config 
- ./dependencies.sh
script:
- ./autogen.sh && ./configure && make
branches:
  only:
  - master
env:
  global:
  - LANG="en_US.UTF-8"
