language: cpp
sudo: required
dist: trusty
compiler:
- clang
os:
- linux
before_install:
- ./dependencies.sh
script:
- ./autogen.sh && ./configure && make
- cat ./Makefile
branches:
  only:
  - master
env:
  global:
  - LANG="en_US.UTF-8"
