language: cpp
sudo: required
dist: trusty
compiler:
- clang
os:
- linux
before_install:
- echo $LANG
- echo $LC_ALL
- ./dependencies.sh
- llvm-config --version
script:
- ./autogen.sh && ./configure && make
branches:
  only:
  - master
env:
  global:
  - LANG="en_US.UTF-8"
